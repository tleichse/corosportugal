# Copy to .env and fill in after creating a project at sanity.io/manage.
# Sanity CLI auto-loads .env files in this folder — no extra config needed.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
